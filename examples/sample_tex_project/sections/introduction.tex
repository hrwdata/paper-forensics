This paper studies a compact auditing pipeline for academic prose. It is important to note that the proposed approach provides a robust framework for rapidly inspecting suspicious language in a paper.

We focus on sentence-level evidence so that a reviewer can read the source material without trusting a single global detector score. The results demonstrate that local evidence is easier to inspect than a single document-wide label.
